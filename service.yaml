apiVersion: v1
kind: Service
metadata:
  name: policy-service-llamaguard-hybrid-v2
  namespace: z-grid
  labels:
    app: policy-service-llamaguard-hybrid-v2
    version: v2.0.0
    component: safety
    tier: service
spec:
  type: ClusterIP
  ports:
  - name: http
    port: 8003
    targetPort: http
    protocol: TCP
  selector:
    app: policy-service-llamaguard-hybrid-v2
  sessionAffinity: None