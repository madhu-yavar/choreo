apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: load-test-metrics-ingress
  namespace: z-grid
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
    nginx.ingress.kubernetes.io/ssl-redirect: "false"
spec:
  rules:
  - host: load-test-metrics.20.242.183.197.nip.io
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: load-test-metrics-api-service
            port:
              number: 8080