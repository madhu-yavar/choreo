apiVersion: apps/v1
kind: Deployment
metadata:
  name: enhanced-jailbreak-service-v7
  namespace: z-grid
  labels:
    app: enhanced-jailbreak-service-v7
    service-type: content-moderation
    version: v7
spec:
  replicas: 1
  selector:
    matchLabels:
      app: enhanced-jailbreak-service-v7
  template:
    metadata:
      labels:
        app: enhanced-jailbreak-service-v7
        service-type: content-moderation
        version: v7
    spec:
      containers:
      - name: enhanced-jailbreak-service-v7
        image: python:3.9-slim
        imagePullPolicy: Always
        command: ["/bin/bash"]
        args: ["-c", "
          apt-get update && apt-get install -y git curl &&
          pip install torch==1.13.1 transformers==4.25.1 scikit-learn flask requests numpy joblib &&
          cd /app &&
          python jailbreak_service_improved_v7.py
        "]
        ports:
        - containerPort: 8002
          name: http
          protocol: TCP
        env:
        - name: SERVICE_NAME
          value: "enhanced-jailbreak-service-v7"
        - name: SERVICE_PORT
          value: "8002"
        - name: SERVICE_HOST
          value: "0.0.0.0"
        - name: MODEL_NAME
          value: "distilbert-base-uncased"
        - name: MAX_SEQUENCE_LENGTH
          value: "512"
        - name: DEVICE
          value: "cpu"
        - name: LOG_LEVEL
          value: "INFO"
        # Enhanced ML thresholds for v7
        - name: ML_CONFIDENCE_HIGH
          value: "0.60"
        - name: ML_CONFIDENCE_MEDIUM
          value: "0.52"
        - name: ML_CONFIDENCE_LOW
          value: "0.48"
        - name: RULE_BOOST_FACTOR
          value: "0.5"
        - name: JAILBREAK_API_KEYS
          value: "supersecret123,jailvalyavar"
        resources:
          requests:
            memory: "512Mi"
            cpu: "200m"
          limits:
            memory: "1Gi"
            cpu: "500m"
        volumeMounts:
        - name: config-volume
          mountPath: /config
          readOnly: true
        livenessProbe:
          httpGet:
            path: /health
            port: 8002
          initialDelaySeconds: 300
          periodSeconds: 60
          timeoutSeconds: 30
          failureThreshold: 3
        readinessProbe:
          httpGet:
            path: /health
            port: 8002
          initialDelaySeconds: 180
          periodSeconds: 30
          timeoutSeconds: 10
          failureThreshold: 3
        startupProbe:
          httpGet:
            path: /health
            port: 8002
          initialDelaySeconds: 60
          periodSeconds: 30
          timeoutSeconds: 10
          failureThreshold: 20
      volumes:
      - name: config-volume
        configMap:
          name: enhanced-jailbreak-service-v7-config
---
apiVersion: v1
kind: Service
metadata:
  name: enhanced-jailbreak-service-v7
  namespace: z-grid
  labels:
    app: enhanced-jailbreak-service-v7
    service-type: content-moderation
spec:
  type: ClusterIP
  ports:
  - name: http
    port: 8002
    targetPort: 8002
    protocol: TCP
  selector:
    app: enhanced-jailbreak-service-v7